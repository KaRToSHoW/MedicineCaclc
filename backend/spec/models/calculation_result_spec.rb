require 'rails_helper'

RSpec.describe CalculationResult, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:calculation_result)
  #   expect(record).to be_persisted
  # end
end
